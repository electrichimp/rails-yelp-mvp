<div class="card p-3 mb-3 mt-4">
  <h2>Escribe tu review de <%= @restaurant.name %></h2>
  <%= simple_form_for [@restaurant, @review] do |f| %>
    <%= f.input :rating %>
    <%= f.input :content %>
    <%= f.submit "Mandar review", class: "btn btn-primary" %>
  <% end %>
</div>
